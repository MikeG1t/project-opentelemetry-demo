# Kafka

This is used as a message queue service to connect the checkout service with
the accounting and fraud detection services.

Kafka is now managed by Confluent Cloud. Environment variables for Confluent Cloud Kafka are substituted at deploy-time.
